# atlassian_rag_exporter — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP + authentication
requests>=2.31.0
tenacity>=8.2.3        # Retry with exponential backoff

# HTML → Markdown conversion
markdownify>=0.13.1
beautifulsoup4>=4.12.3
lxml>=5.1.0            # Fast HTML parser backend for bs4

# YAML config & front-matter
PyYAML>=6.0.1

# Progress bar
tqdm>=4.66.4

# Optional: LlamaIndex integration for direct vector indexing
# llama-index>=0.10.0
# llama-index-readers-confluence>=0.1.3

# Optional: LangChain integration
# langchain>=0.1.0
# langchain-community>=0.0.20

# Optional: OCR for image content extraction
# pytesseract>=0.3.10
# Pillow>=10.0.0
